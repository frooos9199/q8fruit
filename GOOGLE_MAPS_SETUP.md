# 🗺️ دليل إعداد خريطة Google Maps - تتبع المندوبين

## 📋 المتطلبات

تم تفعيل صفحة تتبع المندوبين في لوحة التحكم مع خريطة Google Maps التفاعلية. لتشغيلها، تحتاج إلى مفتاح API من Google.

## 🔑 الحصول على مفتاح Google Maps API

### الخطوة 1: إنشاء مشروع في Google Cloud Console

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com)
2. قم بتسجيل الدخول بحساب Google
3. اضغط على "Select a project" أو "اختر مشروعاً" في الأعلى
4. اضغط على "NEW PROJECT" أو "مشروع جديد"
5. أدخل اسم المشروع (مثل: `Q8-Fruit-Delivery`)
6. اضغط "CREATE" أو "إنشاء"

### الخطوة 2: تفعيل Google Maps APIs

1. في القائمة الجانبية، اذهب إلى **APIs & Services** > **Library**
2. ابحث عن وفعّل الخدمات التالية:
   - ✅ **Maps JavaScript API** (مطلوب)
   - ✅ **Geocoding API** (اختياري - للعناوين)
   - ✅ **Places API** (اختياري - للأماكن)

### الخطوة 3: إنشاء مفتاح API

1. في القائمة الجانبية، اذهب إلى **APIs & Services** > **Credentials**
2. اضغط **+ CREATE CREDENTIALS** > **API key**
3. سيتم إنشاء المفتاح - انسخه فوراً

### الخطوة 4: تأمين المفتاح (مهم جداً!)

1. اضغط على **RESTRICT KEY** أو "تقييد المفتاح"
2. في قسم **Application restrictions**:
   - اختر **HTTP referrers (websites)**
   - أضف النطاقات المسموحة:
     ```
     http://localhost:3000/*
     https://yourdomain.com/*
     https://*.vercel.app/*
     ```
3. في قسم **API restrictions**:
   - اختر **Restrict key**
   - حدد فقط: **Maps JavaScript API**
4. احفظ التغييرات

## ⚙️ إعداد المشروع

### 1. إنشاء ملف .env.local

في المجلد الرئيسي للمشروع، أنشئ ملف `.env.local`:

```bash
cp .env.local.example .env.local
```

### 2. إضافة المفتاح

افتح `.env.local` وأضف مفتاح API:

```env
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSy...your_actual_key_here
```

### 3. إعادة تشغيل الخادم

```bash
npm run dev
```

## 🚀 الاستخدام

### الوصول إلى صفحة التتبع

بعد تسجيل الدخول كمدير، اذهب إلى:
```
/admin/delivery-tracking
```

أو من لوحة التحكم → زر "تتبع المندوبين"

### مميزات الخريطة

- ✅ عرض جميع المندوبين النشطين على الخريطة
- ✅ تحديث تلقائي كل 15 ثانية
- ✅ معلومات تفصيلية عند الضغط على مندوب
- ✅ تكبير وتصغير وعرض القمر الصناعي
- ✅ تتبع السرعة والموقع الدقيق

## 👥 إضافة دور "مندوب"

تم إضافة دور جديد للمستخدمين:

### الأدوار المتاحة:
1. **عميل** - العملاء العاديين
2. **مدير** - إدارة كاملة للنظام
3. **مندوب** - مندوبو التوصيل (جديد! ✨)

### كيفية تعيين مستخدم كمندوب:

1. اذهب إلى **إدارة المستخدمين** في لوحة التحكم
2. اضغط على "تعديل" للمستخدم المطلوب
3. في حقل "الدور"، اختر **"مندوب"**
4. احفظ التغييرات

### عملية التسجيل:

- يمكن لأي شخص التسجيل عادياً (سيكون بدور "عميل")
- المدير يقوم بتغيير الدور إلى "مندوب" من لوحة التحكم
- المندوبون سيحتاجون إلى تطبيق الموبايل لبدء التتبع

## 📱 تطبيق المندوبين (قريباً)

لتفعيل التتبع الكامل، يحتاج المندوبون إلى:

1. تسجيل الدخول في تطبيق الموبايل
2. تفعيل خاصية GPS
3. بدء وضع التوصيل
4. سيظهر موقعهم تلقائياً في لوحة التحكم

## 💰 التكلفة

- ✅ Google Maps يوفر **$200 رصيد مجاني شهرياً**
- ✅ هذا يكفي لـ **28,000 عملية تحميل خريطة** شهرياً
- ✅ لن تحتاج لدفع أي شيء إلا في حالة الاستخدام الكثيف جداً

## 🔒 الأمان

- ✅ المفتاح مقيد بنطاق الموقع فقط
- ✅ لا يمكن استخدامه من مواقع أخرى
- ✅ محمي من السرقة
- ⚠️ **لا تشارك مفتاح API علناً على GitHub**

## ❓ حل المشاكل

### الخريطة لا تظهر

1. تأكد من وجود ملف `.env.local` في المجلد الرئيسي
2. تأكد من صحة مفتاح API
3. تأكد من تفعيل **Maps JavaScript API** في Google Cloud
4. أعد تشغيل خادم Next.js: `npm run dev`
5. افتح Developer Console في المتصفح وتحقق من الأخطاء

### رسالة خطأ في API

- تأكد من عدم وجود حدود على مفتاح API
- تأكد من إضافة نطاق موقعك في HTTP referrers
- انتظر بضع دقائق بعد إنشاء المفتاح (قد يستغرق وقتاً للتفعيل)

### لا يوجد مندوبين على الخريطة

- المندوبون يظهرون فقط عندما يكونون **نشطين**
- تأكد من تفعيل GPS في تطبيق المندوب
- تأكد من أن دورهم "مندوب" في النظام

## 📝 الملفات المعدلة

### 1. إضافة دور "مندوب"
- ✅ `/src/app/admin/users/UserEditModal.tsx`
- ✅ `/src/app/admin/users/UsersTable.tsx`

### 2. خريطة Google Maps
- ✅ `/src/components/DeliveryMap.tsx` (جديد)
- ✅ `/src/app/admin/delivery-tracking/page.tsx` (محدّث)

### 3. التبعيات
- ✅ `@react-google-maps/api` (تم التثبيت)

## 📞 الدعم

إذا واجهت أي مشكلة، تأكد من:
1. مفتاح API صحيح في `.env.local`
2. Maps JavaScript API مفعّل
3. الخادم يعمل بـ `npm run dev`
4. لا توجد أخطاء في Console

---

**تم التطوير بواسطة:** Q8 Fruit Development Team  
**التاريخ:** يناير 2025  
**الإصدار:** 1.0.0
